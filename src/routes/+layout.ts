/** Pre-render all pages at build time (static site generation). */
export const prerender = true;
/** Disable server-side rendering -- the app runs entirely in the browser. */
export const ssr = false;
